# Required in production. Set the verified public HTTPS origin without a trailing path.
NEXT_PUBLIC_SITE_URL=

# Optional public entity and contact values. Leave blank until verified and approved.
NEXT_PUBLIC_LEGAL_NAME=
NEXT_PUBLIC_UNDERWRITING_EMAIL=
NEXT_PUBLIC_CLIENT_SERVICES_EMAIL=
NEXT_PUBLIC_CAREERS_EMAIL=
NEXT_PUBLIC_UNDERWRITING_PHONE=
NEXT_PUBLIC_OFFICE_ADDRESS=
NEXT_PUBLIC_CLIENT_PORTAL_URL=

# Required for production risk-assessment intake. Must be a credential-free HTTPS URL in production.
# The receiver must durably persist or enqueue each accepted submission before returning 2xx,
# deduplicate unchanged retries using Idempotency-Key, retain X-Assessment-Reference,
# assign manual review,
# and track the 24-hour assessment/quotation or status-update target.
RISK_ASSESSMENT_WEBHOOK_URL=

# Required production server-side bearer token for the risk-assessment receiver. Keep secret and
# do not expose it through any NEXT_PUBLIC_* variable.
RISK_ASSESSMENT_WEBHOOK_TOKEN=

# Required for production claims intake.
# The HTTPS receiver must durably persist or enqueue each accepted notification before returning 2xx
# and must deduplicate retries using the Idempotency-Key header.
CLAIMS_NOTIFICATION_WEBHOOK_URL=

# Required independent server-side bearer token for claims delivery. Keep secret and do not expose
# it through any NEXT_PUBLIC_* variable.
CLAIMS_NOTIFICATION_WEBHOOK_TOKEN=
